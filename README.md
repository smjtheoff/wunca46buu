# 📊 LibreNMS Docker Setup

คู่มือการติดตั้งและใช้งาน LibreNMS Network Monitoring System ด้วย Docker Compose

---

## 📋 สารบัญ

- [ภาพรวมโปรเจค](#ภาพรวมโปรเจค)
- [ส่วนประกอบของระบบ](#ส่วนประกอบของระบบ)
- [ข้อกำหนดเบื้องต้น](#ข้อกำหนดเบื้องต้น)
- [การติดตั้งและใช้งาน](#การติดตั้งและใช้งาน)
- [การสร้าง User บน LibreNMS](#การสร้าง-user-บน-librenms)
- [การเข้าถึงระบบ](#การเข้าถึงระบบ)
- [คำสั่งที่ใช้บ่อย](#คำสั่งที่ใช้บ่อย)
- [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## 🎯 ภาพรวมโปรเจค

LibreNMS เป็นระบบ Network Monitoring แบบ Open Source ที่ใช้สำหรับ:
- ติดตามสถานะของอุปกรณ์เครือข่าย (Routers, Switches, Servers)
- แจ้งเตือนเมื่อมีปัญหา (Alert)
- เก็บข้อมูล Performance และ Traffic
- สร้างกราฟและรายงานต่างๆ

โปรเจคนี้ใช้ Docker Compose เพื่อให้การติดตั้งและจัดการง่ายขึ้น

---

## 🧩 ส่วนประกอบของระบบ

โปรเจคประกอบด้วย 4 Services หลัก:

### 1. **MariaDB (db)**
- ฐานข้อมูลสำหรับเก็บข้อมูล LibreNMS
- Port: Internal only (ไม่เปิดออกภายนอก)
- Volume: `./librenms/db` เก็บข้อมูลถาวร

### 2. **Redis (redis)**
- Cache และ Session storage
- ช่วยเพิ่มประสิทธิภาพของระบบ

### 3. **LibreNMS (librenms)**
- Web Interface หลัก
- Port: `8000` (เข้าผ่าน http://localhost:8000)
- Volume: `./librenms/librenms` เก็บ configuration และข้อมูล

### 4. **Dispatcher (dispatcher)**
- ทำงานเบื้องหลัง (Background jobs)
- Poll ข้อมูลจากอุปกรณ์เครือข่าย
- ประมวลผล Alerts และ Discovery

---

## ✅ ข้อกำหนดเบื้องต้น

ก่อนเริ่มต้น ต้องติดตั้งโปรแกรมต่อไปนี้:

- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

ตรวจสอบว่าติดตั้งสำเร็จแล้วด้วยคำสั่ง:
```bash
docker --version
docker-compose --version
```

---

## 🚀 การติดตั้งและใช้งาน

### 1. Clone Repository

```bash
git clone <repository-url>
cd wunca46buu
```

### 2. เข้าไปยัง Directory ที่มี docker-compose.yml

```bash
cd librenms
```

### 3. สร้างและเริ่มต้น Containers

```bash
docker-compose up -d
```

**คำอธิบาย:**
- `up` = สร้างและเริ่มต้น containers
- `-d` = รันในโหมด detached (background)

### 4. ตรวจสอบสถานะ Containers

```bash
docker-compose ps
```

คุณควรเห็น 4 containers ที่มีสถานะ `Up`:
- librenms_db
- librenms_redis
- librenms
- librenms_dispatcher

### 5. ดูการทำงานของ Containers (Logs)

```bash
# ดู logs ของทุก containers
docker-compose logs -f

# ดู logs ของ librenms เท่านั้น
docker-compose logs -f librenms
```

**กด `Ctrl+C` เพื่อออกจากโหมดดู logs**

### 6. รอให้ระบบเริ่มต้นเสร็จสมบูรณ์

รอประมาณ **2-5 นาที** เพื่อให้ระบบเริ่มต้นและสร้างฐานข้อมูลเสร็จสมบูรณ์

---

## 👤 การสร้าง User บน LibreNMS

มี 2 วิธีในการสร้าง Admin User

### วิธีที่ 1: ใช้ Command Line (แนะนำ)

#### สร้าง Admin User แบบ Interactive

```bash
docker exec -it librenms php /opt/librenms/adduser.php
```

จากนั้นระบบจะถามข้อมูล:
1. **Username**: ชื่อผู้ใช้ (เช่น `admin`)
2. **Password**: รหัสผ่าน (จะไม่แสดงตัวอักษรขณะพิมพ์)
3. **Level**: ระดับสิทธิ์ เลือก `10` สำหรับ Admin
4. **Email**: อีเมล (เช่น `admin@example.com`)

#### สร้าง Admin User แบบ One-line Command

```bash
docker exec librenms php /opt/librenms/adduser.php <username> <password> 10 <email>
```

**ตัวอย่าง:**
```bash
docker exec librenms php /opt/librenms/adduser.php admin MySecurePass123! 10 admin@example.com
```

### วิธีที่ 2: ใช้ lnms Command

```bash
docker exec librenms lnms user:add <username> -p <password> --role admin
```

**ตัวอย่าง:**
```bash
docker exec librenms lnms user:add admin -p MySecurePass123! --role admin
```

### ระดับสิทธิ์ (User Levels)

| Level | Role | คำอธิบาย |
|-------|------|----------|
| 10 | Admin | สิทธิ์เต็ม สามารถจัดการทุกอย่างได้ |
| 5 | Global Read | อ่านได้ทุกอย่าง แต่แก้ไขไม่ได้ |
| 1 | Normal User | สิทธิ์จำกัด |

---

## 🌐 การเข้าถึงระบบ

เปิดเว็บเบราว์เซอร์และเข้าไปที่:

```
http://localhost:8000
```

หรือหากรันบน Server อื่น:
```
http://<server-ip>:8000
```

### หน้า Login

1. กรอก **Username** และ **Password** ที่สร้างไว้
2. กด **Login**

### หลังจาก Login สำเร็จ

คุณจะเห็นหน้า Dashboard ที่แสดง:
- Overview ของอุปกรณ์ในระบบ
- Alerts และ Notifications
- กราฟและข้อมูลต่างๆ

---

## 🔧 คำสั่งที่ใช้บ่อย

### จัดการ Docker Compose

```bash
# เริ่มต้น services ทั้งหมด
docker-compose up -d

# หยุด services ทั้งหมด
docker-compose stop

# หยุดและลบ containers (ข้อมูลใน volumes ยังอยู่)
docker-compose down

# หยุดและลบทั้ง containers และ volumes (ระวัง! ข้อมูลจะหายหมด)
docker-compose down -v

# รีสตาร์ท services ทั้งหมด
docker-compose restart

# รีสตาร์ทเฉพาะ librenms
docker-compose restart librenms

# ดู logs
docker-compose logs -f

# ดูสถานะ containers
docker-compose ps
```

### จัดการ LibreNMS

```bash
# เข้า shell ของ librenms container
docker exec -it librenms bash

# รัน validate เพื่อตรวจสอบ configuration
docker exec librenms lnms validate

# อัปเดต LibreNMS
docker-compose pull
docker-compose up -d

# เพิ่มอุปกรณ์ใหม่ผ่าน CLI
docker exec librenms lnms device:add <hostname> --community <snmp-community>

# ดูรายการ users ทั้งหมด
docker exec librenms lnms user:list

# ลบ user
docker exec librenms lnms user:remove <username>
```

### ดูข้อมูล Database

```bash
# เข้า MariaDB shell
docker exec -it librenms_db mysql -u librenms -plibrenms librenms

# หรือใช้ root
docker exec -it librenms_db mysql -u root
```

---

## 🔍 การแก้ไขปัญหา

### ปัญหา: Containers ไม่สามารถเริ่มได้

**วิธีแก้:**
```bash
# ตรวจสอบ logs เพื่อดูข้อผิดพลาด
docker-compose logs

# ลองลบและสร้างใหม่
docker-compose down
docker-compose up -d
```

### ปัญหา: ลืมรหัสผ่าน Admin

**วิธีแก้:** สร้าง admin user ใหม่
```bash
docker exec librenms lnms user:add newadmin -p NewPassword123! --role admin
```

### ปัญหา: Database connection error

**วิธีแก้:**
```bash
# ตรวจสอบว่า db container ทำงานหรือไม่
docker-compose ps

# รีสตาร์ท db และ librenms
docker-compose restart db
docker-compose restart librenms
```

### ปัญหา: Port 8000 ถูกใช้แล้ว

**วิธีแก้:** แก้ไขไฟล์ `docker-compose.yml`
```yaml
ports:
  - "8080:8000"  # เปลี่ยนจาก 8000 เป็น 8080
```

จากนั้น:
```bash
docker-compose down
docker-compose up -d
```

### ปัญหา: ต้องการ Reset ระบบทั้งหมด

**คำเตือน: ข้อมูลทั้งหมดจะถูกลบ**
```bash
docker-compose down -v
rm -rf db/ librenms/
docker-compose up -d
```

---

## 📞 ข้อมูลเพิ่มเติม

### Default Configuration

| Item | Value |
|------|-------|
| Web Port | 8000 |
| Database Name | librenms |
| Database User | librenms |
| Database Password | librenms |
| Timezone | Asia/Bangkok |
| SNMP Community | public |

### Official Documentation

- [LibreNMS Official Docs](https://docs.librenms.org/)
- [LibreNMS Docker Hub](https://hub.docker.com/r/librenms/librenms)
- [Docker Compose Documentation](https://docs.docker.com/compose/)

---

## 📝 License

โปรเจคนี้ใช้งานสำหรับการศึกษาและพัฒนา

---

**Happy Monitoring! 🎉**
